<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ViewModel="clr-namespace:Calculateur_wpf.ViewModel" x:Class="Calculateur_wpf.View.ExportViewWindow"
        Title="ExportViewWindow" Height="300" Width="300">
    <Window.DataContext>
        <ViewModel:ExportViewWindow/>
    </Window.DataContext>
    <Grid>
        <TextBlock Text="{Binding ExportedCharacter, Mode=OneWay, TargetNullValue=Erreur}"/>
    </Grid>
</Window>
